sudo: required
services:
  - docker

before_install:
  - docker build -t muralikrishnankh/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true muralikrishnankh/react-test npm test

after_success:
  - docker build -t muralikrishnankh/multi-client ./client
  - docker build -t muralikrishnankh/multi-nginx ./nginx
  - docker build -t muralikrishnankh/multi-server ./server
  - docker build -t muralikrishnankh/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push muralikrishnankh/multi-clien
  - docker push muralikrishnankh/multi-nginx
  - docker push muralikrishnankh/multi-server
  - docker push muralikrishnankh/multi-worker
